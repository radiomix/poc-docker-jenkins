{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "security_group_id": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-west-2",
    "source_ami": "ami-24c62044",
    "instance_type": "t1.micro",
    "ssh_username": "ubuntu",
    "tags": {
	"Name": "jenkins docker-chef-base-container-UB14-04-{{timestamp}}"
    },
    "ami_name": "docker-chef-base-container-UB-14-04-{{timestamp}}",
    "security_group_id": "{{user `security_group_id`}}"
  }],
  "provisioners": [{
  "type": "shell",
    "environment_vars": [
      "version={{user `version`}}"
      ],
    "scripts": ["scripts/docker.sh"]
  }
 ]
}
