{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "security_group_id": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-west-2",
    "source_ami": "ami-fa9cf1ca",
    "instance_type": "t1.micro",
    "ssh_username": "ubuntu",
    "run_tags": {
	"name": "base-ami-dockerize-chef{{timestamp}}"
    },
    "ami_name": "base-ami-docker-chef{{timestamp}}",
    "security_group_id": "{{user `security_group_id`}}"
  }],
  "provisioners": [{
    "type": "shell",  
    "inline": [
      "sleep 30",
      "sudo apt-get update",
      "sudo apt-get install -y wget",
      "sudo apt-get install -y curl",
      "sudo apt-get install -y ssh",
      "sudo apt-get install -y vi",
      "sudo apt-get install -y git",
      "sudo apt-get install -y mosh",
      "sudo apt-get install -y lynx",
      "sudo apt-get install -y unzip",
      "sudo apt-get install -y debconf-utils",
      "which lynx mosh git vi wget unzip",
      "sudo apt-get install -y linux-image-generic-lts-raring linux-headers-generic-lts-raring",
      "sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 36a1d7869245c8950f966e92d8576a8ba88d21e9",
      "sudo sh -c 'echo deb http://get.docker.io/ubuntu docker main > /etc/apt/sources.list.d/docker.list'",
      "sudo uname -a"
    ]
  },
  {
  "type": "shell",  
    "inline": [
      "sleep 10",
      "echo WAITING for 10 sec"
    ]
  },
  {
  "type": "file",  
  "source": "hosts.im7",
  "destination": "/tmp/hosts.im7"
  },
  {
  "type": "shell", 
    "inline": [
     "sleep 30",
     "echo ADDING im7 HOST NAMES to etc hosts",
     "sudo bash -c  'cat /tmp/hosts.im7 >> /etc/hosts'; more /etc/hosts",
     "rm -f /tmp/hosts.im7",
     "mkdir docker",
     "cd docker && git clone http://github.com/radiomix/poc-docker-jenkins.git",
     "cd docker && git clone http://github.com/dotcloud/docker-registry.git",
     "cd docker/docker-registry && git checkout 0.7.0",
     "cd",
     "curl -l https://www.opscode.com/chef/install.sh | sudo bash",
     "echo INSTALLING DOCKER",
     "sudo apt-get update",
     "sudo apt-get install -y lxc-docker",
     "sudo gpasswd -a ubuntu docker",
     "sudo service docker restart",
     "docker --version",
     "sudo apt-get -y upgrade"
   ]
  },
  {
  "type": "shell",
    "inline": [
      "echo BUILDING REGISTRY BASE CONTAINER TAKES 20 MINUTES",
      "cd  /home/ubuntu/docker/poc-docker-jenkins/container_builds/registry/ && ./build.sh -b",
      "docker images",
      "echo WAITING for 10 sec"
    ]
  }
 ]
}
